import { CloseModalOnClickOutisdeDirective } from './close-modal-on-click-outisde.directive';

describe('CloseModalOnClickOutisdeDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseModalOnClickOutisdeDirective();
    expect(directive).toBeTruthy();
  });
});
